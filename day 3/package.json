{
  "name": "day-3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "json-server --watch data.json --port 8080"
  },
  "author": "shakti",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
